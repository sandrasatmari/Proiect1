@page
@model Proiect1.Pages.CompaniiAeriene.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanieAeriana[0].Nume_Airline)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanieAeriana[0].Tara)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanieAeriana[0].CallCenter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanieAeriana[0].EmailContact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.CompanieAeriana)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nume_Airline)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tara)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CallCenter)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EmailContact)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
